// Generated by LiveScript 1.2.0
(function(){
  define(['repository', 'template!./items', 'template!./item', 'template!./itemBrief', 'jquery', 'underscore', 'uuid', 'util'], function(repo, temp, itemp, briefT, $, _, uuid, util){
    var addItem, addComment, addUp, addDown, countDown, countUp, countComment, iDownIt, iUpIt, iCommentIt;
    addItem = function(text){
      var item;
      item = {
        text: text,
        id: new uuid().toString(),
        author: repo.me().id,
        date: new Date(),
        version: 1
      };
      repo.postItem(item);
      return item;
    };
    addComment = function(refId, text){
      var item;
      item = {
        text: text,
        id: new uuid().toString(),
        author: repo.me().id,
        date: new Date(),
        version: 1,
        ref: refId,
        type: 'comment'
      };
      repo.postItem(item);
      return item;
    };
    addUp = function(refId){
      var item;
      item = {
        text: ':up',
        id: new uuid().toString(),
        author: repo.me().id,
        date: new Date(),
        version: 1,
        ref: refId,
        type: 'up'
      };
      repo.postItem(item);
      return item;
    };
    addDown = function(refId){
      var item;
      item = {
        text: ':down',
        id: new uuid().toString(),
        author: repo.me().id,
        date: new Date(),
        version: 1,
        ref: refId,
        type: 'down'
      };
      repo.postItem(item);
      return item;
    };
    countDown = function(refId){
      return repo.findItem({
        type: 'down',
        ref: refId
      }).length;
    };
    countUp = function(refId){
      return repo.findItem({
        type: 'up',
        ref: refId
      }).length;
    };
    countComment = function(refId){
      return repo.findItem({
        type: 'comment',
        ref: refId
      }).length;
    };
    iDownIt = function(refId){
      return repo.findItem({
        type: 'down',
        ref: refId,
        author: repo.me().id
      }).length > 0;
    };
    iUpIt = function(refId){
      return repo.findItem({
        type: 'up',
        ref: refId,
        author: repo.me().id
      }).length > 0;
    };
    iCommentIt = function(refId){
      return repo.findItem({
        type: 'comment',
        ref: refId,
        author: repo.me().id
      }).length > 0;
    };
    return {
      view: function(){
        var displayItem, updateItem;
        displayItem = function(it){
          var item, ref, comments;
          item = (it.authorObj = repo.friend(it.author), it.timeAgo = util.timeAgo(it.date), it.downNum = countDown(it.id), it.upNum = countUp(it.id), it.commentNum = countComment(it.id), it.iDownIt = iDownIt(it.id), it.iUpIt = iUpIt(it.id), it.iCommentIt = iCommentIt(it.id), it.isComment = it.type === 'comment', it.hasComments = false, it);
          if (item.isComment) {
            ref = repo.item(item.ref);
            item.commentRef = {
              picture: repo.friend(ref.author).picture,
              text: ref.text,
              textBrief: util.brief(ref.text, 35)
            };
            item.briefTemplate = briefT;
          }
          if (item.commentNum > 0) {
            comments = repo.findItem({
              type: 'comment',
              ref: item.id
            });
            item.hasComments = true;
            item.comments = _(comments).map(function(it){
              return {
                picture: repo.friend(it.author).picture,
                text: it.text,
                textBrief: util.brief(it.text, 35)
              };
            });
            item.briefTemplate = briefT;
          }
          return $(itemp(item));
        };
        updateItem = function(refId){
          var i, item;
          i = $('.items-list').find(".list-group-item[data-id=" + refId + "]");
          item = repo.item(refId);
          return i.replaceWith(displayItem(item));
        };
        $('.items').append($(temp()));
        _(repo.items()).chain().sortBy(function(it){
          return -new Date(it.date);
        }).each(function(it){
          if (it.type === 'up' || it.type === 'down') {} else {
            return $('.items-list').append(displayItem(it));
          }
        });
        $('.text-send').val('');
        $('.btn-send').click(function(){
          return addItem($('.text-send').val());
        });
        $('.text-send').keydown(function(it){
          var item;
          if (it.which === 13) {
            it.preventDefault();
            item = addItem($('.text-send').val());
            return $('.items-list').prepend(displayItem(item));
          }
        });
        $('.items-list').on('keydown', '.text-comment', function(e){
          var tf, text, refId, item;
          if (e.which === 13) {
            e.preventDefault();
            tf = $(e.target);
            text = tf.val();
            refId = tf.closest('.list-group-item').data('id');
            item = addComment(refId, text);
            $('.items-list').prepend(displayItem(item));
            return updateItem(refId);
          }
        });
        $('.items-list').on('click', '.up-button', function(e){
          var refId, item;
          refId = $(e.target).closest('.list-group-item').data('id');
          item = addUp(refId);
          return updateItem(refId);
        });
        $('.items-list').on('click', '.down-button', function(e){
          var refId, item;
          refId = $(e.target).closest('.list-group-item').data('id');
          item = addDown(refId);
          return updateItem(refId);
        });
        $('.items-list').on('click', '.expand-button', function(e){
          var itemD, ref$;
          itemD = $(e.target).closest('.list-group-item');
          if ((ref$ = itemD.find('.comment-ref')) != null) {
            ref$.toggleClass('hide');
          }
          return (ref$ = itemD.find('.comments')) != null ? ref$.toggleClass('hide') : void 8;
        });
        $('.items-list').on('click', '.comment-button', function(e){
          return $(this).closest('.list-group-item').find('.comment-text').toggleClass('hide');
        });
        return $('.items-list').on('mouseenter mouseleave', '.list-group-item', function(e){
          $(e.target).closest('.list-group-item')[e.type === 'mouseenter' ? 'addClass' : 'removeClass']('hover');
          return e.preventDefault();
        });
      }
    };
  });
}).call(this);
